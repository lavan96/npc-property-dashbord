import { ReactNode } from 'react';

interface CashFlowConstructionScheduleProps {
  children: ReactNode;
}

export function CashFlowConstructionSchedule({ children }: CashFlowConstructionScheduleProps) {
  return <>{children}</>;
}
