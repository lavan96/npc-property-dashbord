import { ReactNode } from 'react';

interface CashFlowChartsSectionProps {
  children: ReactNode;
}

export function CashFlowChartsSection({ children }: CashFlowChartsSectionProps) {
  return <>{children}</>;
}
