import { ReactNode } from 'react';

interface CashFlowComparisonBarProps {
  children: ReactNode;
}

export function CashFlowComparisonBar({ children }: CashFlowComparisonBarProps) {
  return <>{children}</>;
}
