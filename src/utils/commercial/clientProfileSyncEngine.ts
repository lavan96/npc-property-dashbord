import type { ClientProfile, ClientScenario, ClientScenarioAuditEvent } from './clientPortfolioTypes';
export function buildClientProfileImportAudit(field: string, previousValue: unknown, newValue: unknown, scenarioId?: string): ClientScenarioAuditEvent { return { timestamp: new Date().toISOString(), user: 'Calculator user', action: 'Client profile value imported', field, previousValue, newValue, source: 'Client Profile Source', scenarioId }; }
export function commitScenarioToClientProfile(client: ClientProfile, scenario: ClientScenario): ClientProfile { return { ...client, scenarios: [...client.scenarios.filter(s => s.scenarioId !== scenario.scenarioId), { ...scenario, status: 'Committed', auditLog: [...scenario.auditLog, { timestamp: new Date().toISOString(), user: 'Calculator user', action: 'Scenario committed to client profile', source: 'Commercial / Industrial calculator', scenarioId: scenario.scenarioId }] }], latestBorrowingCapacity: scenario.resultingPosition.borrowingCapacity }; }
